{
	"name": "Existing Dockerfile",
	"context": "..",
	"dockerFile": "../Dockerfile",
	"workspaceMount": "src=${localWorkspaceFolder},dst=/app,type=bind,consistency=delegated",
	"workspaceFolder": "/app",

	"runArgs": [
		// "-u", "vscode"
		"--name", "otto-engine-vscode"
	],

	"settings": { 
		"terminal.integrated.shell.linux": "/bin/bash"
	},

	"appPort": [],

	"extensions": [
		"ms-python.python"
	]
}
